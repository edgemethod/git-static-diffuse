extends layout

block content
	p.page-type Repository
	h1
		a(href="#{baseUrl}") / 
		= repo
	p Available branches:
	ul
		each branch in branches
			li
				a(href="#{baseUrl}/#{repo}/#{branch}")= branch
				ul
					li
						a(href="#{baseUrl}/#{repo}/#{branch}/build/index.html")= "latest"
